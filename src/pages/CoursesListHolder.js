import { Card, CardBody, CardHeader, CardText, CardTitle } from "reactstrap";
import CourseList from "../@core/components/Course/CourseList";

const CoursesListHolder = () => {
  return <CourseList />;
};

export default CoursesListHolder;
